[package]
name = "role-api"
version = "0.1.0"
edition = "2024"
publish = false

[features]
default = []
client = ["macroni/client"]
server = ["macroni/server"]

[dependencies]
macroni = { path = "../..", default-features = false }
serde = { version = "1.0", features = ["derive"] }

